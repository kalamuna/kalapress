# a temporary solution for wpengine until
# we have our setup for automattically adapting build scripts to platform(wpe, pantheon, etc)
cd wp-content/themes/kalapress-theme && composer install --optimize-autoloader --no-dev --no-progress
